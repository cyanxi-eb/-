# 面试背记学习卡 v2.7 计划表

> 在 v2.6（194 题）基础上，把题库拆为 **4 个主题库（4 级压缩）**，并入 903/904 增量知识。
> 路径沿用当前 `面试题/面试背记学习卡v2.6/`（改造后即升 v2.7，无需新建目录）。

---

## 〇、需求拆解

### 0.1 用户原话

> 至少 3 个主题库：1 号全是必学/高星题无删减；2 号压缩去细枝；3 号去分枝只保留主干 <200；4 号在 3 号基础上再压缩 <100。
> 拓展题用同样的方式分题库。
> 首次打开可选择题库；后续在编辑中切换。
> 新版本记为 V2.7，并用 pdf/image 技能把 `Desktop/903` 与 `Desktop/904` 内容入库。

### 0.2 我对需求的落地化

| 主题库 | 含义 | 包含 | 题量目标 | 备注 |
|--------|------|------|---------|------|
| **库1 完整版** | 必学 + 高星全量 | 必刷全集(4-5星) + 拓展全集(1-3星) | **194** 题 | 当前 v2.6 全集，零删减 |
| **库2 标准版** | 轻度压缩 | 去除 1星冷门 + 2星次要 + 4星细分 | **≈160–175** 题 | 适合 1-2 周短期复习 |
| **库3 精简版** | 只留主干 | 在库2基础上去掉非高频 | **<200** 题 | 实际目标 **≈120–150** 题 |
| **库4 极简版** | 主干再压 | 在库3基础上只留最核心 5星+高频4星 | **<100** 题 | 实际目标 **≈60–90** 题 |

库 3 和库 4 都已满足「<200」「<100」上限（用户给的是上限，不是必须接近）。

---

## 一、数据字段改造

### 1.1 新增字段 `bank`

每题加一个数字字段，**表示该题最高保留到几号库**（数字越大越核心）：

| 值 | 含义 | 出现于 |
|----|------|--------|
| `bank: 1` | 最次要 | 仅库1 |
| `bank: 2` | 中等次要 | 库1 + 库2 |
| `bank: 3` | 次核心 | 库1 + 库2 + 库3 |
| `bank: 4` | 核心 | 全部 4 个库 |

**筛选规则**：主题库 N 的题集 = 所有 `bank >= N` 的题。

### 1.2 降级策略（初版，可在脚本里微调）

| 原星级 | 原类型 | 默认 bank | 说明 |
|--------|--------|----------|------|
| 5 星 | 全部 | 4 | 核心必考，保留到最简版 |
| 4 星（主干） | 高频考点 | 4 | 库4 也要保留 |
| 4 星（细分） | 单函数/语法细节 | 3 | 砍到库3以后 |
| 3 星 | 拓展全部 | 3 | 保留到库3 |
| 2 星 | 软技能（高频） | 3 | 保留到库3 |
| 2 星 | 人事面 | 2 | 保留到库2 |
| 1 星 | 人事面冷门 | 1 | 仅库1 |

预期分布（基于 194 题）：

| bank | 估计题数 | 累计 = 库 |
|------|---------|----------|
| 4 | ~94 | 库4 = 94 ✓ <100 |
| 3 | ~50 | 库3 = 144 ✓ <200 |
| 2 | ~30 | 库2 = 174 |
| 1 | ~20 | 库1 = 194 |

> 最终题数由 `tools/bank-assign.cjs` 自动分配，可人工微调后再跑构建。

### 1.3 新增题目（903/904 入库）

基于 903 + 904 的图片与文字内容，提取以下新题（全部默认 `bank: 4`，因为都是高频面试题）：

| 编号 | 来源 | 分类 | 题码 | 题目 |
|------|------|------|------|------|
| 1 | 903 上午笔记 | 软技能 | SK-9 | IT 行业岗位分工与协作流程 |
| 2 | 903 上午笔记 | Linux | LNX-12 | Git 分支合并的标准流程 |
| 3 | 903 redis三大高可用模式.txt + 图1 | 缓存与Redis | DB-56 | Redis 三大高可用模式（主从/哨兵/Cluster）架构对比 |
| 4 | 903 图2 | 缓存与Redis | DB-57 | Redis 与数据库双写一致性的三种策略 |
| 5 | 903 图3 | 缓存与Redis | DB-58 | 缓存雪崩的成因与解决方案 |
| 6 | 903 图5 | 缓存与Redis | DB-59 | 缓存穿透与缓存击穿的区分与解决方案 |
| 7 | 903 图4 | 缓存与Redis | DB-60 | RDB 快照与 AOF 日志的对比 |
| 8 | 903 图6 | 大模型应用 | LLM-17 | 高可用 AI 应用架构分层（展示/网关/服务/基础/运维） |
| 9 | 903 上午笔记 | MySQL | DB-61 | MySQL 索引为什么用 B+ 树（vs 哈希/B 树/红黑树） |
| 10 | 903 上午笔记 | MySQL | DB-62 | MySQL 分库分表的常见方案（mycat）与适用场景 |
| 11 | 904 PNG | 大模型应用 | LLM-18 | RAG 知识库如何处理 PDF 多类型文档（文本/扫描件/表格） |
| 12 | 904 PNG | 大模型应用 | LLM-19 | RAG 文档切块（Chunking）策略与边界处理 |
| 13 | 904 PNG | 大模型应用 | LLM-20 | 主流文档解析工具对比（MinerU / Docling / LightningParse / DocForge / pdfplumber） |
| 14 | 904 langChain.txt | 大模型应用 | LLM-21 | LangChain 核心组件与典型工作流 |

预估 **14 题**，全部 `bank: 4`（核心题）。

---

## 二、数据真源与文件结构

### 2.1 真源结构（保持单文件 + 分片双形态）

**真源**：`questions.json`（顶层数组，每题带 `bank` 字段），保持不变。

**构建产物**：

| 形态 | 路径 | 内容 |
|------|------|------|
| 单文件版 | `dist/面试背记学习卡v2.7.html` | `<script>window.__FC_BANKS = {1:[...], 2:[...], 3:[...], 4:[...]};</script>` |
| 分片 PWA 版 | `dist/web/banks/bank-1.json` … `bank-4.json` | 4 个 JSON，按需加载 |
| PWA 元数据 | `dist/web/banks.manifest.json` | `{banks: [1,2,3,4], counts: {1:194, 2:174, 3:144, 4:94}}` |

### 2.2 题库加载策略

```
首次打开 / 编辑模式切换题库
  ↓
检测 localStorage.v27_activeBanks 是否存在
  ├─ 不存在 → 弹"选择题库"对话框，默认全选 [1,2,3,4]
  └─ 存在 → 直接按已存的选择加载
  ↓
按选择从单文件 __FC_BANKS 或分片 bank-N.json 读取
  ↓
App.data = 选中 bank 的题集合并 + 按 id 重新排序
  ↓
进入抽背/记背/指导/编辑四模式
```

---

## 三、UI 改造

### 3.1 首次打开：选择题库弹层

- 模态遮罩 + 居中卡片
- 4 个 checkbox：`库1 完整(194题) / 库2 标准(~174题) / 库3 精简(~144题) / 库4 极简(~94题)`
- 底部"取消 / 确定"；默认全选
- 选中状态写入 `localStorage.v27_activeBanks`

### 3.2 编辑模式：切换题库入口

- 编辑模式顶部工具栏新增按钮：`📚 切换题库`
- 点击复用首次打开的弹层
- 切换后提示"已切换至：库1+库3 (238 题)"，刷新列表

### 3.3 进度持久化（按激活的 bank 组合为 key）

- `key = 'v27_progress_' + activeBanks.sort().join('-')`
- 例：选了 1 和 3 → key = `v27_progress_1-3`
- 切换题库后进度互不干扰
- 当 progress 在当前组合下为空时，正常显示"0 已掌握"

### 3.4 不改动的部分

- 四模式框架（抽背/记背/指导/编辑）不变
- 必刷/拓展开关、分类筛选、搜索、薄弱复习、导出功能保留
- 编辑 CRUD 仍然直接编辑 `App.data`（已合并的当前题集）

---

## 四、构建脚本改造（build-v25.cjs → 仍用同名，逻辑改）

```
build-v25.cjs（文件名保留，避免破坏既有命令行习惯）
  │
  ├─ loadData()                ← 读 questions.json，新增 bank 字段校验
  ├─ buildSingle(data)
  │    └─ 注入 window.__FC_BANKS = {1:[bank>=1], 2:[bank>=2], 3:[bank>=3], 4:[bank>=4]}
  │       （4 次 filter，性能足够；也可以一次 reduce）
  ├─ buildWeb(data)
  │    ├─ 生成 banks/bank-1.json, bank-2.json, bank-3.json, bank-4.json
  │    ├─ 生成 banks.manifest.json
  │    ├─ sw.js ASSETS 加 4 个 bank json + manifest
  │    └─ data-loader.js 改为：先读 manifest，按已选 bank 列表按需 fetch
  └─ 产物改名 面试背记学习卡v2.7.html + manifest version "2.7"
```

新构建一次产出：

- `dist/面试背记学习卡v2.7.html`
- `dist/web/index.html` + `dist/web/css/*.css` + `dist/web/js/*.js`（js 已合并到 index.html / 走 importmap）
- `dist/web/banks/bank-1.json` ... `bank-4.json`
- `dist/web/banks.manifest.json`
- `dist/web/sw.js`（缓存新资源）

---

## 五、数据工具脚本

新增 `tools/bank-assign.cjs`：

```js
// 读 questions.json → 按规则给每题分配 bank（1/2/3/4）
// 规则表：
//   5星 → 4
//   4星 + 高频关键词 → 4
//   4星 + 细分关键词（"枚举函数"/"魔术方法"/"正则.*?"等）→ 3
//   3星 → 3
//   2星 + 软技能高频 → 3
//   2星 + 人事面 → 2
//   1星 → 1
// 写回 questions.json，并打印各 bank 分布
```

新增 `tools/903-904-import.cjs`：

```js
// 把上面 14 道新题合并进 questions.json，自动分配下一个 id 和题码
// 题码冲突检测；幂等可重跑
```

---

## 六、文件清单（新增 / 修改）

### 新增

| 文件 | 作用 |
|------|------|
| `tools/bank-assign.cjs` | 给现有 194 题分配 bank |
| `tools/903-904-import.cjs` | 导入 903/904 新题 |
| `v2.7计划表.md` | 本文件 |

### 修改

| 文件 | 改动 |
|------|------|
| `questions.json` | 新增 `bank` 字段 + 新增 14 题 |
| `build-v25.cjs` | 改产物名 v2.7；输出 4 个 bank；sw.js 缓存新资源 |
| `src/index.html` | 加选择题库弹层 DOM + 标题 v2.7 |
| `src/js/store.js` | 新增 key：`v27_activeBanks`、`v27_progress_*` |
| `src/js/data-loader.js` | 支持多 bank 按需加载；返回合并后的 App.data |
| `src/js/app.js` | 首次打开弹选择题库；编辑模式入口 |
| `src/js/editor.js` | 工具栏加 "📚 切换题库" 按钮 |
| `src/css/base.css`（或新建 bank-picker.css） | 弹层样式 |
| `README.md` | 同步 v2.7 + 4 主题库说明 + 手动修改 bank 规范 |
| `CHANGELOG.md` | 追加 v2.7 章节 |

---

## 七、题量自检目标

构建完成后必须达成：

| 主题库 | 目标题量 | 实际题量（构建后断言） |
|--------|---------|----------------------|
| 库1（全集） | 194 + 14 = **208** | 等于 208 |
| 库2 | ≤ 200 | < 200 |
| 库3 | < 200 | < 200 |
| 库4 | < 100 | < 100 |

`build-v25.cjs` 末尾加自检断言，不通过则 exit 1。

---

## 八、里程碑与节奏

| 阶段 | 内容 | 验证 |
|------|------|------|
| **M1** | 写完本计划文档，等用户审阅 | 用户确认后启动 |
| **M2** | `bank-assign.cjs` + 跑分配 + 校验题量 | 各 bank 题数打印 + JSON 合法 |
| **M3** | `903-904-import.cjs` 导入 14 题 | questions.json 题数 194 → 208 |
| **M4** | 构建脚本改造 + 产出 v2.7 单文件 + 分片 | 自检通过 |
| **M5** | UI：选择题库弹层 + 编辑模式切换 | 首次打开弹层截图 + 切换刷新截图 |
| **M6** | README + CHANGELOG 同步 | 文档审阅 |
| **M7** | 全功能回归（抽背/记背/指导/编辑/导出/进度/搜索） | 手工点一遍 |

---

## 九、风险与遗留

- **风险 1**：bank 字段在编辑模式被改后存到 localStorage，可能与下次构建的 questions.json 冲突。处理：编辑模式不暴露 bank 字段修改（银行字段是构建期产物，运行期只读）。
- **风险 2**：切换题库后老 bank 的进度变成"幽灵数据"占 localStorage。处理：保留 30 天内的所有进度 key，过期清理（暂不实现，留 TODO）。
- **风险 3**：903/904 的图片识别可能漏字。处理：用户对每题人工二次确认一遍（不入自动入库流程）。
- **遗留**：build-v25.cjs 文件名未改（沿用历史），版本号在脚本里写 "2.7"；待 v3.0 真正重构时再改名。

---

*等待用户审阅本文档后进入 M2。*
