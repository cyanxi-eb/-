# 更新文档 / 修改规范

> 本文件记录版本演进、数据/代码修改规范，供后续维护者参考。
> 与 `README.md` 的「手动修改规范」互为补充：README 讲"怎么改"，本文档讲"为什么 + 改什么 + 历史"。

---

## 一、版本历史

| 版本 | 日期 | 变更要点 |
|------|------|---------|
| v2.4 | 2026-08-21 | 上一代单文件版：280 题 / 25 分类 / 三模式，数据内联 |
| bh-mindmaps | 2026-08-27 | 目录型项目：272 题 / 18 分类，UI/数据分离 + PWA + 399 图 |
| **v2.5** | 2026-08-27 | 融合重构版：442 题 / 14 大类，模块化 + 双形态 + 四模式 |
| **v2.5.1** | 2026-08-28 | 题库治理：分类合并（消除 <5 道的小类）+ 重要性分级（1-5 星）+ 必刷/拓展筛选 |
| **v2.5.2** | 2026-08-28 | 删除计算机视觉(图片损坏)+Docker；「必刷/拓展」改双开关支持三种组合；控制栏横向排布 |
| **v2.5.3** | 2026-08-31 | 知识库更新：新增 Nginx/HTTP协议/大模型应用 三分类 + 扩充缓存与Redis，共 36 题（422 题 / 15 大类） |
| **v2.5.4** | 2026-09-03 | 题库瘦身：删拓展题(除软技能/人事面)+删必刷冗余题+降级，422 → 275 题（必刷 195 / 拓展 80） |
| **v2.6** | 2026-09-03 | 面试导向重构：删所有算法题 + 大幅简化 ML/DL/Transformer + 全库以"通过面试"为唯一目标精简，275 → 194 题（必刷 144 / 拓展 50） |
| **v2.7** | 2026-09-05 | 多主题库重构：4 级压缩（完整/标准/精简/极简）+ 首次选择题库 + 编辑切换 + 并入 903/904/905/LLM0903 增量 + 加回 v2.0~v2.4 重要高频题 |
| **v2.8** | 2026-09-06 | 云端同步：可选接入 Supabase，昵称登录后学习进度/设置/编辑题库自动上云，换设备不丢（未接入时回退纯本地） |

---

## 二、v2.5 相对上一代的架构变化

| 维度 | v2.4 / bh-mindmaps | v2.5 |
|------|-------------------|------|
| 数据存放 | 内联 HTML / 分片 JS | `questions.json` 单一真源 |
| 代码组织 | 单文件混写 | `src/css` + `src/js` 分模块 |
| 题码 | `id` 整数 / "1."混编 | `{PREFIX}-{N}` 统一题码 |
| 交付 | 单文件 或 目录 | 单文件 + dist 分片双形态 |
| 编辑 | 无 | 第四模式（CRUD + 存档回滚） |
| 图片 | 399 PNG 引用 | 内容提取（P0，进行中） |

---

## 三、修改规范（后续维护）

### 3.1 数据修改统一入口

所有题目改动只发生在 `questions.json`。流程：

```
改 questions.json → node build-v25.cjs → 产物更新 → 提交
```

### 3.2 版本号规则

- 题面/答案的**内容勘误**（改字、补漏）→ 不升版本，直接改
- **新增分类/新增功能/架构调整** → 升次版本（v2.5 → v2.6），并在此 CHANGELOG 追加记录
- 每次升版本需同步更新：`README.md` 版本说明 + 单文件版 `<title>` + 本文档版本表

### 3.3 提交检查清单（改完必查）

- [ ] `questions.json` 合法 JSON（可被 `JSON.parse` 解析）
- [ ] `code` 唯一、`id` 唯一
- [ ] 题码序号无补零、无跳号冲突
- [ ] 已运行 `node build-v25.cjs` 重新构建
- [ ] 单文件版 `file://` 双击可打开
- [ ] 无手改 dist 产物

### 3.4 数据审查机制

`tools/merge.cjs` 会输出 `tools/_review_report.json`，包含以下审查标记：

| 标记 | 含义 | 处理方式 |
|------|------|---------|
| 代码块未闭合 | answer 中 ` ``` ` 数量为奇数（可能撕裂） | 人工检查代码块，补齐或合并 |
| 答案过短 | answer < 20 字符 | 人工补全 |
| 题目为空 | question 为空 | 删除或补题目 |
| 疑似编码乱码 | 含 GBK 误读字符 | 修正乱码字 |
| 无 PREFIX 映射 | 分类未在映射表 | 在 merge.cjs 补映射 |

### 3.5 撕裂代码处理规范（bh-mindmaps 遗留）

bh-mindmaps 部分题目存在"同一内容被隔断成多块"的撕裂问题。处理规则：

1. 检测：answer 代码块未闭合、或连续多题 question 高度相似且 answer 各自残缺
2. 处理：将断片**合并回一道题**，题码取首题，其余删除
3. 无法确认归属的断片 → 标记 `source: "bh-frag"` 暂存，不并入正式题库

---

## 四、数据来源与去重状态

| 数据源 | 题数 | 状态 |
|--------|------|------|
| v2.4（主体） | 280 | ✅ 已并入 |
| bh-mindmaps 互补分类 | 162 | ✅ 已并入（经清洗） |
| bh-mindmaps 重叠分类 | 110 | ⏳ 暂存 `tools/_bh_overlap.json`，待去重后并入 |
| PDF 独有题 | 待比对 | ⏳ P0-B 后续（`tools/extract-pdf.py`） |

> 重叠分类 = bh-mindmaps 中与 v2.4 同领域的 Python基础/高级、数据处理、机器学习、深度学习、Linux，需与 v2.4 逐题比对去重后再并入，避免重复。

### 4.1 已修复的数据质量问题（2026-08-27）

| 问题 | 处理 | 脚本 |
|------|------|------|
| 6 道纯 LeetCode 链接题（两数之和/最长回文/LRU/爬楼梯/翻转二叉树/反转链表） | 补全为「题目+思路+代码+复杂度」完整题卡 | `tools/fix-ds.cjs` |
| 排序算法/递归用栈/set 复杂度/软技能 5 题过简 | 补全为完整答案 | `tools/fix-ds.cjs` |
| DS 12 道「问题描述/参考答案」撕裂碎片（与补全题重复） | 删除 | `tools/fix-tear.cjs` |
| NLP 5 条 question/answer 错位记录 | 重构为 13 道正确题卡 | `tools/fix-tear.cjs` |
| HR-17 题目被截断（续句掉进答案开头） | 续句归位 | `tools/fix-tear2.cjs` |
| CV-10/CV-11 同一道题被拆两半 | 合并删 CV-11 | `tools/fix-tear2.cjs` |

### 4.3 题库治理（2026-08-28，v2.5.1）

| 项 | 处理 | 结果 |
|----|------|------|
| 分类过细（<5 道） | 流程控制/异常处理/文件操作/网络编程/模块与包 → 合并进「Python基础」 | 所有分类 ≥5 道 |
| 重要性分级 | 加 `star` 字段（1-5 星），`tools/refine.cjs` 自动分级 | 5星91 / 4星208 / 3星95 / 2星33 / 1星10 |
| 必刷/拓展筛选 | UI 加「拓展题」开关，默认只看 4-5 星必刷 | 必刷 299 题 / 拓展 138 题 |

**星级规则**（`tools/refine.cjs`）：核心关键词命中 → 5 星；冷门关键词（社保/家庭/业余爱好等）→ 1 星；人事面/软技能 → 2 星；其余 v2.4=4 星、bh=3 星。

### 4.4 知识库更新（2026-08-31，v2.5.3）

依据「新知识来源.txt（9 个有道云课件 + 低代码/RAG 笔记）+ MODEL.png/RAG.png（Transformer 架构、RAG 工作流）」更新题库，新增 36 题（386 → 422 题 / 15 大类）。

| 来源 | 分类 | 新增 | 题码 |
|------|------|------|------|
| 有道云《Redis 课件》 | 缓存与Redis（扩充） | +5 | DB-51~55 |
| 有道云《Nginx 课件》 | Nginx（新增） | +8 | NG-1~8 |
| 有道云《http协议入门》 | HTTP协议（新增） | +7 | HTTP-1~7 |
| 笔记《低代码/RAG/Agent》+ RAG图 | 大模型应用（新增） | +9 | LLM-1~9 |
| 有道云《微调实操/预训练模型》 | 大模型应用（新增） | +7 | LLM-10~16 |

**新增脚本：** `tools/add-new.cjs`（新题数据 + 合并逻辑，幂等追加）。

**关于「以新知识为准」：** 新知识中与现有题重复的部分（Transformer 架构 DL-25、Seq2Seq DL-18、标准化归一化 ML-31~36、FastAPI 33 题、Python 数据分析 49 题）经比对**现有答案已完整覆盖**，未重复新增，仅对新增领域做了补充。

**来源标记：** `yd` = 有道云笔记课件；`note` = 用户 txt 笔记 + 图片解读。

### 4.5 题库瘦身（2026-09-03，v2.5.4）

用户反映 422 题过多，按三步瘦身到 275 题（必刷 195 / 拓展 80）。

| 步骤 | 操作 | 数量 |
|------|------|------|
| ① 删拓展题 | 删除拓展题中除「软技能/人事面」以外的全部 | -53 |
| ② 删必刷冗余 | 删除低价值/过细/重复的必刷题（合并进保留题） | -94 |
| ③ 降级 | 剩余仍多，把次要 4 星题降为 3 星（移到拓展） | -37 |

**必刷 326 → 195 题（≤200 ✓）。**

**瘦身脚本：** `tools/slim.cjs`（DELETE_BS 清单 94 题 + DOWNGRADE 清单 37 题，可追溯）。

**删除原则：** 低价值题（发展历史/安装导入/黑盒等）、过细题（单个元字符/单个函数）、重复题（同类合并保留核心）。核心必考（索引/事务/Transformer/LoRA/反向代理/负载均衡等 5 星题）全部保留。

**分类整理：** 合并后「正则表达式」并入「Python基础」、「机器学习-数据处理」并入「机器学习」；「数据库与ORM」分类随拓展题删除移除。

**注意：** 被删的拓展题中，少数为高频考点（缓存三大问题 DB-50、QKV NLP-2、词向量 NLP-4 等），如后续需要可单独升星保留。

### 4.6 面试导向重构（2026-09-03，v2.6）

以「通过面试」为唯一目标，不考虑笔试与实际技能提升，275 → 194 题（必刷 144 / 拓展 50）。

| 操作 | 内容 |
|------|------|
| 删算法题 | 数据结构算法分类（LeetCode 题）10 题全删，笔试不考虑 |
| 大幅简化模型算法 | ML/DL/Transformer 删公式推导、实现细节、过深内容（注意力公式、√dk、正规方程、交叉熵推导、Teacher Forcing、XLNet、BERT+CRF 等） |
| 其他题面试导向精简 | NumPy/Pandas 删函数属性细节；MySQL/Linux 删语法细节/底层实现；FastAPI/大模型应用删工具实操 |
| 版本更名 | 全产品命名 v2.6（标题/单文件/分片 manifest/PWA） |

**保留原则：** 只留面试会问的概念性、对比性、原理性高频题（如"索引/事务/隔离级别""Transformer 架构/位置编码""RNN/LSTM/GRU""过拟合/反向传播""装饰器/闭包/GIL"），删"公式怎么推""API 怎么调""代码怎么写"这类笔试/实操内容。

**分类合并：** 「深度学习-循环神经网络」「注意力与Transformer」并入「深度学习」；「NLP基础」并入「大模型应用」；「NumPy」并入「Pandas」；「线性回归」并入「机器学习」。

**脚本：** `tools/v26-slim.cjs`（DELETE 81 题清单 + 分类合并，可追溯）。

### 4.7 v2.7 开发：加回 v2.0~v2.4 重要高频题（2026-09-05）

用户在 v2.6 精简后发现删过头，要求从 v2.0~v2.4 历史题卡中挑重要题加回（v2.0~v2.4 各版本题目均包含于 v2.4 全集 280 题内，故以 `tools/_v24.json` 为源）。

**加回 39 题**（194 → 233 题），筛选原则：面试高频 + 当前题库缺失，避开用户明确删除的 Docker / 算法题 / 公式推导 / 工具实操。

| 分类 | 加回 | 代表题 |
|------|------|--------|
| MySQL | +9 | 窗口函数/排名函数/不适合建索引/JOIN优化/UUID主键/Buffer Pool/CPU打满排查等 |
| 并发编程 | +3 | Queue vs Manager、孤儿/僵尸进程、Thread vs Process |
| 面向对象 | +3 | super()、类与对象、封装机制 |
| 流程控制/综合进阶 | +3 | while/for/range/enumerate、for 底层实现 |
| NumPy/Pandas | +4 | 布尔/花式索引、apply、pivot_table、读写 CSV/Excel |
| Linux | +4 | su -、find、crontab、df/du |
| 机器学习 | +4 | 三要素、建模流程、测试集只用 transform、召回率vs精确率 |
| 深度学习 | +5 | vs传统ML、人工神经元、RNN→Transformer、因果/填充掩码、残差+层归一化 |
| FastAPI | +4 | response_model、Query/Path/Body、APIRouter、Form |

**星级**：5星 10 + 4星 20 + 3星 9（必刷 30 / 拓展 9）。

**来源标记**：`source: "v2.4-reimport"`，用于区分「重新加回」与原始 v2.4 题。

**脚本**：`tools/reimport-important.cjs`（关键词匹配 `_v24.json` → 分配 code/id/star → 幂等追加）。

### 4.8 v2.7 增量导入：903 / 904 / 905 / LLM0903（2026-09-05）

用户补充四个新知识源，经与现有题库逐项去重后，只导入「当前缺失的高频面试题」共 **11 题**（233 → 244 题）。

| 来源 | 内容 | 新增题码 | 分类 |
|------|------|---------|------|
| 905 `内容.txt` | Git 分支工作流（创建→切换→编码→提交推送→合并 master） | SK-9 | 软技能 |
| 903 `上午.txt` + 图 | Redis 三大高可用架构 / 穿透击穿雪崩 / RDB-AOF / 双写一致性 / 淘汰策略 | DB-64~68 | 缓存与Redis |
| LLM0903（RAG.md/langchain.md/Less01.ipynb 实操） | LangChain 是什么 / LCEL 链式调用 / LangChain 实现 RAG 代码流程 / RAG vs 微调 | LLM-16~19 | 大模型应用 |
| 904 `RAG 知识库 pdf 解析多个类型文件.png` | PDF 多类型（纯文本/扫描件/表格）解析策略 | LLM-20 | 大模型应用 |

**去重说明**：903 的 MySQL 考点（B+树/索引/引擎/执行计划/分库分表）、904 的 LangChain 链接、LLM0903 的 RAG 原理/分块/向量库——现有题库已覆盖（DB-19~24、DB-31/57/58/60、LLM-2/3/5/6 等），未重复新增。

**星级**：5星 4 + 4星 7（全部必刷，Redis 三大问题与 PDF 解析为高频考点）。

**来源标记**：`source` 分别标记 `905` / `903` / `904` / `LLM0903`。

**脚本**：`tools/import-905-llm.cjs`（新题数据 + 幂等追加，按 code 去重）。

> 注：903 `redis三大高可用模式.txt`、904 `langChain.txt`、905 中的链接为 URL 引用，正文已由 LLM0903 的 RAG.md / langchain.md 完整覆盖，故以本地文档为准。

### 4.9 v2.7 核心：多主题库（bank 分级）+ 首次选择题库（2026-09-05）

**数据层**：每题加 `bank` 字段（1-4，值越大越核心），主题库 N = 所有 `bank >= N` 的题。分配脚本 `tools/bank-assign.cjs`（显式题码清单 CORE/MINOR/COLD/HR_COLD，幂等可重跑）。

| 主题库 | 含义 | 题量 |
|--------|------|------|
| 库1 完整版 | 必刷+拓展全量，零删减 | 244 |
| 库2 标准版 | 去冷门与细微分枝 | 209 |
| 库3 精简版 | 只留主干高频 | 174（<200 ✓） |
| 库4 极简版 | 面试冲刺最核心 | 95（<100 ✓） |

**构建层**（`build-v25.cjs`）：单文件版注入 `window.__FC_BANKS = {1..4}`（单档无重叠）+ `__FC_BANK_META`；分片版输出 `banks/bank-{1..4}.json` + `banks.manifest.json`；产物名 v2.7；`sw.js` 缓存新资源；末尾自检断言（库3<200、库4<100，不通过 exit 1）。

**UI 层**：
- `store.js` 新增 `v27_activeBanks` + 动态 key（`progressKey/bankKey/archivesKey/oplogKey`，库1 沿用 `v25_*` 老 key 兼容老进度）
- `data-loader.js` 改为加载 `__FC_BANKS` / `banks/*.json`，回调返回分档题集
- `app.js` 新增 `mergeBanks/applyActiveBank/openBankPicker/confirmBankPicker`，首次打开弹「选择题库」对话框（单选 4 档，默认库1）
- `index.html` 加题库选择弹窗 + toast 提示 DOM，标题 v2.7
- `editor.js` 工具栏加「📚 切换题库」按钮
- `base.css` 加弹窗/toast 样式

**设计说明**：4 个库是「递进包含」关系（库4 ⊂ 库3 ⊂ 库2 ⊂ 库1），故选择题库采用**单选**（非多选），更符合"压缩档位"语义。

**产物**：`dist/面试背记学习卡v2.7.html`（365 KB）+ `dist/web/`（4 档 banks + manifest + sw.js）。

### 4.10 v2.8 核心：云端同步（Supabase + 简单昵称登录，2026-09-06）

用户反馈「本地部署太麻烦，想要远程登录 + 记录数据」，引入可选的云端同步能力。

**架构**：静态前端仍托管在 GitHub Pages 等任意静态平台，数据存储接入 Supabase（BaaS，免费额度），**无需自建服务器**。登录方式采用「简单昵称」（用户选定），数据范围为「全部含编辑」（用户选定）。

| 层 | 改动 |
|----|------|
| 数据模型 | Supabase `profiles` 表：一行一个昵称，`data(jsonb)` 存该用户全部状态（进度/档位/主题/字体/编辑题库） |
| 前端 | 新增 `src/js/cloud.js`：纯 `fetch` 调 Supabase PostgREST（不引 SDK，单文件/分片版通用），实现 `login/pullAndApply/push(防抖)/logout/collect/applyToLocal` |
| 持久化 | `store.js` 的 `set()` 末尾调 `Cloud.markDirty()`，登录态下自动防抖推送（停止操作 0.5s 后） |
| 初始化 | `app.js` 的 `init()`：已登录先 `pullAndApply()` 拉云端覆盖本地再启动；未登录/失败回退本地 |
| UI | 顶部栏「🔑 登录」按钮（登录后显示 👤 昵称）+ 登录弹窗（输入昵称）；`base.css` 加登录样式 |
| 构建 | `JS_FILES` 加 `cloud.js`、`VERSION=2.8`、产物名 `v2.8.html`、`sw.js` 缓存 `fc-v28` + `./js/cloud.js` |

**同步范围**：学习进度、题库档位、主题/字体、编辑后的题库。**不同步**：编辑器「存档点/操作日志」（本地撤销辅助，体积大价值低）。排除规则 `_shouldSync()`（`v25_`/`v27_` 前缀 + 排除 `archives`/`oplog`/`nickname`）。

**优雅降级**：`cloud.js` 顶部 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 为空时 `enabled=false`，回退纯本地，一切功能照旧。

**安全边界**：昵称无密码 + anon key 公开 → 数据「防丢失」不「防偷看」，任何人输同昵称可读该昵称数据（学习进度不敏感，够用）。真·私有账号需 Supabase Auth，属后续升级。

**文档**：新增 `docs/supabase-setup.md`（注册 → 建项目 → 建表 SQL → 填 key → 重建部署），含完整建表 SQL 与常见问题。

**产物**：`dist/面试背记学习卡v2.8.html`（377 KB）+ `dist/web/`（含 `js/cloud.js`）。

### 4.2 遗留待修复清单（后续里程碑处理）

| # | 问题 | 位置 | 计划 |
|---|------|------|------|
| 1 | 错字「性+格」应为「性格」 | HR-23 题面 | 数据勘误 |
| 2 | 错字「神经网路」应为「神经网络」 | CV-10 题面 | 数据勘误 |
| 3 | CV-10 答案过简（learning-rate 等参数设置要点） | CV-10 | 扩展补全 |
| 4 | HR 人事面 5 道过短话术题（HR-11/15/18/23/33） | 人事面 | 选择性扩展 |
| 5 | v2.4 数据约 45 处「+」号连接符（如「标准库+第三方库」「输入+偏置」） | 全库 v2.4 题 | 统一为「、」或「和」 |

> 说明：#5 的「+」是 v2.4 原作者用「+」作并列连接符的表达习惯，非编码乱码；可读但建议后续统一规范化。

---

## 五、里程碑进度

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M1 P0-A | 图内容提取 | ⏳ 待做 |
| M2 P0-B | PDF 并库 + 题码统一 | 🔶 进行中（442 题基础库已建，PDF 对比/去重待续） |
| M3 P0-C | 指导模式重构 | ⏳ 待做 |
| M4 | UI 升级 + 构建脚本 | 🔶 进行中（骨架已建） |
| M5 | PWA 远程化 | ⏳ 待做 |
| M6 | 编辑模式 + 代码换行 | ⏳ 待做 |
| M7 | 高亮 + 全功能回归 | ⏳ 待做 |

---

*本文档随每次版本升级同步更新。*
