# 更新文档 / 修改规范

> 本文件记录版本演进、数据/代码修改规范，供后续维护者参考。
> 与 `README.md` 的「手动修改规范」互为补充：README 讲"怎么改"，本文档讲"为什么 + 改什么 + 历史"。

---

## 一、版本历史

| 版本 | 日期 | 变更要点 |
|------|------|---------|
| v2.4 | 2026-08-21 | 上一代单文件版：280 题 / 25 分类 / 三模式，数据内联 |
| bh-mindmaps | 2026-08-27 | 目录型项目：272 题 / 18 分类，UI/数据分离 + PWA + 399 图 |
| **v2.5** | 2026-08-27 | 融合重构版：442 题 / 14 大类，模块化 + 双形态 + 四模式 |

---

## 二、v2.5 相对上一代的架构变化

| 维度 | v2.4 / bh-mindmaps | v2.5 |
|------|-------------------|------|
| 数据存放 | 内联 HTML / 分片 JS | `questions.json` 单一真源 |
| 代码组织 | 单文件混写 | `src/css` + `src/js` 分模块 |
| 题码 | `id` 整数 / "1."混编 | `{PREFIX}-{N}` 统一题码 |
| 交付 | 单文件 或 目录 | 单文件 + dist 分片双形态 |
| 编辑 | 无 | 第四模式（CRUD + 存档回滚） |
| 图片 | 399 PNG 引用 | 内容提取（P0，进行中） |

---

## 三、修改规范（后续维护）

### 3.1 数据修改统一入口

所有题目改动只发生在 `questions.json`。流程：

```
改 questions.json → node build-v25.cjs → 产物更新 → 提交
```

### 3.2 版本号规则

- 题面/答案的**内容勘误**（改字、补漏）→ 不升版本，直接改
- **新增分类/新增功能/架构调整** → 升次版本（v2.5 → v2.6），并在此 CHANGELOG 追加记录
- 每次升版本需同步更新：`README.md` 版本说明 + 单文件版 `<title>` + 本文档版本表

### 3.3 提交检查清单（改完必查）

- [ ] `questions.json` 合法 JSON（可被 `JSON.parse` 解析）
- [ ] `code` 唯一、`id` 唯一
- [ ] 题码序号无补零、无跳号冲突
- [ ] 已运行 `node build-v25.cjs` 重新构建
- [ ] 单文件版 `file://` 双击可打开
- [ ] 无手改 dist 产物

### 3.4 数据审查机制

`tools/merge.cjs` 会输出 `tools/_review_report.json`，包含以下审查标记：

| 标记 | 含义 | 处理方式 |
|------|------|---------|
| 代码块未闭合 | answer 中 ` ``` ` 数量为奇数（可能撕裂） | 人工检查代码块，补齐或合并 |
| 答案过短 | answer < 20 字符 | 人工补全 |
| 题目为空 | question 为空 | 删除或补题目 |
| 疑似编码乱码 | 含 GBK 误读字符 | 修正乱码字 |
| 无 PREFIX 映射 | 分类未在映射表 | 在 merge.cjs 补映射 |

### 3.5 撕裂代码处理规范（bh-mindmaps 遗留）

bh-mindmaps 部分题目存在"同一内容被隔断成多块"的撕裂问题。处理规则：

1. 检测：answer 代码块未闭合、或连续多题 question 高度相似且 answer 各自残缺
2. 处理：将断片**合并回一道题**，题码取首题，其余删除
3. 无法确认归属的断片 → 标记 `source: "bh-frag"` 暂存，不并入正式题库

---

## 四、数据来源与去重状态

| 数据源 | 题数 | 状态 |
|--------|------|------|
| v2.4（主体） | 280 | ✅ 已并入 |
| bh-mindmaps 互补分类 | 162 | ✅ 已并入（经清洗） |
| bh-mindmaps 重叠分类 | 110 | ⏳ 暂存 `tools/_bh_overlap.json`，待去重后并入 |
| PDF 独有题 | 待比对 | ⏳ P0-B 后续（`tools/extract-pdf.py`） |

> 重叠分类 = bh-mindmaps 中与 v2.4 同领域的 Python基础/高级、数据处理、机器学习、深度学习、Linux，需与 v2.4 逐题比对去重后再并入，避免重复。

### 4.1 已修复的数据质量问题（2026-08-27）

| 问题 | 处理 | 脚本 |
|------|------|------|
| 6 道纯 LeetCode 链接题（两数之和/最长回文/LRU/爬楼梯/翻转二叉树/反转链表） | 补全为「题目+思路+代码+复杂度」完整题卡 | `tools/fix-ds.cjs` |
| 排序算法/递归用栈/set 复杂度/软技能 5 题过简 | 补全为完整答案 | `tools/fix-ds.cjs` |
| DS 12 道「问题描述/参考答案」撕裂碎片（与补全题重复） | 删除 | `tools/fix-tear.cjs` |
| NLP 5 条 question/answer 错位记录 | 重构为 13 道正确题卡 | `tools/fix-tear.cjs` |

### 4.2 遗留待修复清单（后续里程碑处理）

| # | 问题 | 位置 | 计划 |
|---|------|------|------|
| 1 | 错字「性+格」应为「性格」 | HR-23 题面 | 数据勘误 |
| 2 | 错字「神经网路」应为「神经网络」 | CV-10 题面 | 数据勘误 |
| 3 | CV-10 答案过简（learning-rate 等参数设置要点） | CV-10 | 扩展补全 |
| 4 | HR 人事面 5 道过短话术题（HR-11/15/18/23/33） | 人事面 | 选择性扩展 |
| 5 | v2.4 数据约 45 处「+」号连接符（如「标准库+第三方库」「输入+偏置」） | 全库 v2.4 题 | 统一为「、」或「和」 |

> 说明：#5 的「+」是 v2.4 原作者用「+」作并列连接符的表达习惯，非编码乱码；可读但建议后续统一规范化。

---

## 五、里程碑进度

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M1 P0-A | 图内容提取 | ⏳ 待做 |
| M2 P0-B | PDF 并库 + 题码统一 | 🔶 进行中（442 题基础库已建，PDF 对比/去重待续） |
| M3 P0-C | 指导模式重构 | ⏳ 待做 |
| M4 | UI 升级 + 构建脚本 | 🔶 进行中（骨架已建） |
| M5 | PWA 远程化 | ⏳ 待做 |
| M6 | 编辑模式 + 代码换行 | ⏳ 待做 |
| M7 | 高亮 + 全功能回归 | ⏳ 待做 |

---

*本文档随每次版本升级同步更新。*
