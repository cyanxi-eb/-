window.__fcParts = window.__fcParts || [];
window.__fcParts.push([{"id":421,"code":"FS-30","category":"FastAPI","question":"Q25：CORS跨域是什么?如何解决?","answer":"**【核心概念】** 跨域资源共享(CORS)是一种浏览器安全机制,用于允许运行在一个源(Origin)的Web应用,通过浏览器向另一个源的服务器发起跨域HTTP请求,并在服务器授权的前提下获取资源。\n\n**【原理机制】** **同源的三个条件**:协议 + 域名 + 端口(三者完全相同才算同源)\n\n跨域场景:前端 `http://localhost:5173/`(Vue)→ 后端 `http://127.0.0.1:8000/`(FastAPI),端口不同,属于跨域。\n\n**解决方案**:CORS中间件——让后端主动告诉浏览器:\"这个前端允许访问\"。\n\n**【代码示例】**\n```python\nfrom fastapi.middleware.cors\nimport CORSMiddlewareorigins = [    \"http://localhost\",    \"http://localhost:3000\",    \"https://your-frontend-domain.com\"]\napp.add_middleware(    CORSMiddleware,    allow_origins=[\"*\"],       # 允许访问的源    allow_credentials=True,    # 允许携带Cookie    allow_methods=[\"*\"],       # 允许所有请求方法    allow_headers=[\"*\"],       # 允许所有请求头)\n```\n**【项目实践】** AI掘金头条在 `main.py` 中全局配置CORS中间件,允许前端Vue(5173端口)访问后端FastAPI(8000端口)。\n\n**【知识联动】** 前置知识:中间件(Q6)\n\n后续知识:前后端联调关联概念:CORS是浏览器行为,服务器端配置响应头 `Access-Control-Allow-Origin` **⚠️ 面试官可能追问** 生产环境中 `allow_origins=[\"*\"]` 安全吗?→ 不安全,应指定具体域名预检请求(OPTIONS)是什么?→ 浏览器在发送非简单请求前先发OPTIONS确认服务器是否允许","extend":"","source":"bh"},{"id":422,"code":"FS-31","category":"FastAPI","question":"Q26：新闻列表接口的完整实现流程是怎样的?","answer":"**【核心概念】** 新闻列表接口是项目中最复杂的查询接口,涉及分页查询、聚合统计、缓存策略的综合应用。\n\n**【原理机制】** 完整流程:\n```\n进入请求 → 处理分页规则 → 按分类ID查询新闻 → 获取新闻总数量 → 计算是否有更多新闻 → 响应结果\n```\n各步骤详解:\n| 步骤 | 实现方式 |\n| --- | --- |\n| 分页规则 | 跳过数量 = (页码-1) × 每页数量 |\n| 按分类查询 | select(News).where(News.category_id == category_id).offset(skip).limit(page_size) |\n| 获取总量 | select(func.count()).where(News.category_id == category_id) |\n| 是否有更多 | (offset + 当前新闻列表条数) < 总量 |\n响应结果包含:当前分类新闻列表、新闻总量(total)、是否还有更多新闻(hasMore)。\n\n**【代码示例】**\n```python\n@router.get(\"/list\")\nasync def get_news(    category_id: int = Query(..., alias=\"categoryId\"),    page: int = 1,    page_size: int = Query(10, le=100, alias=\"pageSize\"),    db: AsyncSession = Depends(get_db)):    # 1. 计算跳过数量    skip = (page - 1) * page_size        # 2. 查询当前分类新闻(分页)\nstmt = select(News).where(News.category_id == category_id).offset(skip).limit(page_size)\nresult =\nawait db.execute(stmt)\nnews_list = result.scalars().all()        # 3. 获取总量    count_stmt = select(func.count()).select_from(News).where(News.category_id == category_id)\ntotal_result =\nawait db.execute(count_stmt)\ntotal = total_result.scalar()        # 4. 判断是否有更多    has_more = (skip + len(news_list)) < total\nreturn success_response(data={        \"list\": news_list,        \"total\": total,        \"hasMore\": has_more    })\n```\n**【项目实践】** 结合缓存后的完整流程:先查Redis → 有缓存直接返回 → 无缓存查数据库 → 写入Redis → 返回。\n\n**【知识联动】** 前置知识:分页查询(Q13)、聚合查询(Q13)、旁路缓存(Q21)\n\n后续知识:缓存Key设计(Q22)\n\n关联概念:`Query(..., alias=\"categoryId\")` 实现参数名映射","extend":"","source":"bh"},{"id":423,"code":"FS-32","category":"FastAPI","question":"Q27：收藏模块和浏览历史模块如何实现幂等性?两者的区别是什么?","answer":"**【核心概念】** 幂等性指同一操作执行多次与执行一次效果相同。收藏模块通过 `UniqueConstraint` 保证唯一性;浏览历史通过\"先查后更新\"实现幂等。\n\n**【原理机制】** **收藏模块**:\n\n使用 `UniqueConstraint('user_id', 'news_id')` 确保同一用户不会重复收藏同一新闻添加收藏前无需检查,数据库约束自动防重 **浏览历史模块**:\n\n添加浏览记录时先检查是否已浏览过当前新闻已浏览过:更新浏览时间(不重复插入)\n\n未浏览过:添加新记录两者功能对比:\n| 功能 | 收藏模块 | 浏览历史模块 |\n| --- | --- | --- |\n| 添加 | 添加收藏 | 添加浏览记录（幂等：已有则更新时间） |\n| 取消/删除 | 取消收藏 | 删除单条浏览记录 |\n| 列表 | 获取收藏列表（分页） | 获取浏览历史列表（分页） |\n| 清空 | 清空收藏列表 | 清空浏览历史列表 |\n| 额外功能 | 检查收藏状态 | — |\n**【代码示例】**\n```python\n# 收藏模块:检查是否已收藏\n@router.get(\"/check\")\nasync def check_favorite(    news_id: int = Query(..., alias=\"newsId\"),    user: User = Depends(get_current_user),    db: AsyncSession = Depends(get_db)):    result =\nawait db.execute(        select(Favorite).where(            Favorite.user_id == user.id,            Favorite.news_id == news_id        )    )\nis_favorited = result.scalars().first()\nis not None\nreturn success_response(data={\"isFavorited\": is_favorited})# 浏览历史:先查后更新\n@router.post(\"/add\")\nasync def add_history(data: HistoryAddRequest, user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)):    # 检查是否已浏览过    existing =\nawait db.execute(        select(History).where(History.user_id == user.id, History.news_id == data.news_id)    )\nrecord = existing.scalars().first()\nif record:        # 已浏览:更新浏览时间        record.view_time = datetime.now()\nelse:        # 未浏览:添加记录        db.add(History(user_id=user.id, news_id=data.news_id))\nawait db.commit()\nreturn success_response(message=\"添加成功\")\n```\n**【项目实践】** 收藏列表和浏览历史列表都使用联表查询:`select(News).join(Favorite/History)`,获取关联的新闻详情。清空操作使用 `delete()` + `result.rowcount` 返回删除条数。\n\n**【知识联动】** 前置知识:联表查询(Q14)、UniqueConstraint(Q14)、CRUD(Q12)\n\n后续知识:依赖注入认证(Q16)\n\n关联概念:`result.rowcount` 获取操作影响的行数","extend":"","source":"bh"},{"id":424,"code":"FS-33","category":"FastAPI","question":"Q28：QWen大模型如何集成到项目中?","answer":"**【核心概念】** AI掘金头条通过阿里云百炼模型广场集成通义千问(Qwen)系列模型,前端直接调用模型API,实现AI摘要、智能推荐等功能。\n\n**【原理机制】** 集成步骤:\n| 步骤 | 操作 |\n| --- | --- |\n| 1 | 登录阿里云百炼模型广场 |\n| 2 | 选择模型（通义千问3-Max） |\n| 3 | 查看API参考，获取HTTP请求地址 + API Key |\n| 4 | 前端配置调用 |\n前端配置位置:`项目前端 → src/ → config/ → api.js → 替换大模型HTTP请求地址 + API Key + 模型名称` **【项目实践】** AI掘金头条前端通过配置文件指定模型接口,支持千问系列模型的使用。前端调用时直接指定对应的API地址和Key,无需后端中转。\n\n**【知识联动】** 前置知识:项目工程结构(Q24)、前后端分离架构后续知识:可扩展为后端代理调用(保护API Key)\n\n关联概念:当前方案是前端直调,生产环境建议后端代理以保护Key安全 **⚠️ 面试官可能追问** 前端直调API Key安全吗?→ 不安全,生产环境应通过后端代理转发如果模型响应慢怎么办?→ 可使用流式响应(StreamingResponse)\n\n**面试复习路线图**\n| 阶段 | 目标 | 重点题目 | 建议时间 |\n| --- | --- | --- | --- |\n| 初级 | 掌握FastAPI基础概念和用法 | Q1-Q5, Q9, Q25 | 3-5天 |\n| 中级 | 掌握进阶特性和ORM，能独立完成CRUD | Q6-Q8, Q10-Q14, Q15-Q18 | 5-7天 |\n| 高级 | 掌握缓存设计、项目架构、性能优化 | Q19-Q23, Q24, Q26-Q28 | 5-7天 |\n**知识网络全景图**\n```\nFastAPI基础(Q1-Q5)    ├── 异步编程 → 异步ORM(Q10)→ 异步Redis(Q20)    ├── 路由参数 → Pydantic验证(Q4)→ 响应格式(Q5/Q17)    └── 响应类型 → 异常处理(Q18)FastAPI进阶(Q6-Q9)    ├── 中间件 → CORS(Q25)    ├── 依赖注入 → 数据库会话(Q10)→ 用户认证(Q16)    └── 模块化路由 → 项目工程结构(Q24)ORM与数据库(Q10-Q14)    ├── 配置 → 模型定义 → CRUD    ├── 条件/聚合/分页查询 → 新闻列表(Q26)    └── 联表查询 → 收藏/浏览历史(Q27)安全认证(Q15-Q18)    ├── passlib加密 → Token认证 → 用户模块全流程    └── 通用响应 + 全局异常 → 项目规范缓存与Redis(Q19-Q23)    ├── Redis配置 → 旁路缓存 → Key设计    └── 穿透/击穿/雪崩 → 性能优化项目综合(Q24-Q28)    ├── 工程结构 → 模块实现 → 缓存集成    └── AI模型调用 → 完整项目交付\n```","extend":"","source":"bh"},{"id":425,"code":"NLP-1","category":"NLP基础","question":"介绍一下 BERT？BERT 和 BiLSTM 有什么区别？BERT 和 Attention 是什么关系？","answer":"**BERT（Bidirectional Encoder Representations from Transformers）：** 基于 Transformer Encoder 堆叠的双向预训练语言模型，通过 MLM（掩码语言模型）+ NSP（下一句预测）两个任务在大规模语料上预训练，再微调下游任务。\n\n**BERT vs BiLSTM：**\n- BiLSTM 是循环结构，串行处理、难以并行，长距离依赖靠隐状态逐层传递；BERT 用自注意力，可并行、直接建模任意两词间关系\n- BiLSTM 通常从零训练；BERT 大规模预训练 + 微调，泛化能力更强\n\n**BERT 和 Attention 的关系：** BERT 的 Encoder 内部核心就是多头自注意力（Multi-Head Self-Attention），Attention 是 BERT 的基本组成单元。","extend":"**面试追问：** BERT 是双向（看上下文两侧），GPT 是单向（只看左侧），这是两者本质区别；BERT 适合理解类任务（分类/NER），GPT 适合生成类任务。","source":"bh-fixed"},{"id":426,"code":"NLP-2","category":"NLP基础","question":"解释 Q、K、V 分别的作用？","answer":"**Q（Query）** 代表查询张量，**K（Key）** 代表关键信息张量，**V（Value）** 代表实际取值张量。注意力机制先通过 Q 与 K 的交互（点积）计算匹配权重，再将权重作用到 V 上进行加权求和，得到最终输出。\n\n- **Q**：当前词在\"找什么\"（想关注哪些信息）\n- **K**：各词\"有什么\"可被关注的特征\n- **V**：各词实际携带的信息内容","extend":"**面试追问：** 自注意力里 Q=K=V 都来自同一输入（经不同权重矩阵 Wq/Wk/Wv 线性变换）；交叉注意力里 Q 来自目标、K/V 来自源。点积除以 √d_k 是为了防止维度大导致 softmax 梯度消失。","source":"bh-fixed"},{"id":427,"code":"NLP-3","category":"NLP基础","question":"介绍一下 GloVe？","answer":"2013 年 Google 提出 Word2Vec 后，斯坦福 NLP 小组于 2014 年提出 GloVe（Global Vectors for Word Representation）。\n\n此前词向量主要有两大类：全局矩阵分解（如 LSA，有效利用统计信息但类比任务表现差）与局部上下文窗口（如 skip-gram，类比好但少用全局统计）。\n\nGloVe 是基于全局词频统计的模型，实现三步：\n- 构建词-词共现矩阵\n- 建立词向量与共现矩阵的近似关系\n- 构造损失函数训练\n\nGloVe 结合了全局矩阵分解与局部上下文窗口两者的优点，只训练共现矩阵中的非零元素，有效利用统计信息，生成的向量空间有意义的子结构。","extend":"**面试追问：** GloVe 属于「计数类」词向量，Word2Vec 属于「预测类」；两者都难处理一词多义，之后被 ELMo/BERT 等上下文相关表示取代。","source":"bh-fixed"},{"id":428,"code":"NLP-4","category":"NLP基础","question":"词向量是什么？有哪些方式生成词向量？句子的词向量怎么表示？","answer":"词向量是自然语言的数字化表示，方便计算机处理。主要有三种模式：\n- **one-hot**：稀疏、维度爆炸、无语义\n- **Word2Vec / GloVe**：静态稠密向量，含语义，但一词一向量、无法处理多义\n- **Word Embedding（BERT 系列）**：上下文相关，同一词在不同语境向量不同\n\n句子的词向量表示：\n- 分词后各单词词向量的加权平均\n- 直接取 BERT 输出层的 [CLS] 向量（本质就是句向量）","extend":"**面试追问：** Word2Vec 有 CBOW（用上下文预测中心词）和 skip-gram（用中心词预测上下文）两种；静态词向量无法区分\"苹果（水果）\"和\"苹果（公司）\"。","source":"bh-fixed"},{"id":429,"code":"NLP-5","category":"NLP基础","question":"说一下前缀树（Trie）？","answer":"前缀树又称单词查找树、Trie 树，是一种树形结构，是哈希树的变种。典型应用是统计、排序和保存大量字符串，常用于搜索引擎的文本词频统计。\n\n**核心思想：** 空间换时间，利用字符串公共前缀降低查询时间开销。\n\n**三个基本特性：**\n- 根节点不含字符，其余节点只含一个字符\n- 从根到某节点路径上的字符连接起来，就是该节点对应的字符串\n- 每个节点的所有子节点字符互不相同\n\n搜索时从根节点开始逐字符匹配，查询效率高于哈希树。","extend":"**面试追问：** Trie 常用于自动补全、拼写检查、IP 路由最长前缀匹配；缺点是空间占用大，可用压缩 Trie（Patricia）或双数组 Trie 优化。","source":"bh-fixed"},{"id":430,"code":"NLP-6","category":"NLP基础","question":"讲讲迁移学习、特征迁移和微调迁移？","answer":"**迁移学习：** 构建新模型时，把已有预训练模型作为起点直接继承，达到\"站在巨人肩膀上\"的效果。\n\n**特征迁移（Feature-representation-transfer）：** 找到有代表性的特征，通过特征变换把源域和目标域数据映射到同一空间，使两者分布一致。\n\n**微调迁移（Fine-tuning）：** 把预训练模型作为训练起点，在特定数据集上做二次微调训练。","extend":"**面试追问：** 大模型时代主流就是「预训练 + 微调」范式；微调可只更新顶层（冻结主干）或全参数微调，数据少时用 LoRA 等参数高效微调。","source":"bh-fixed"},{"id":431,"code":"NLP-7","category":"NLP基础","question":"BERT 和 Transformer 的区别？","answer":"BERT 是基于 Transformer 架构的模型，但有几个关键差异：\n- BERT 只用 Transformer 的 **Encoder** 部分，舍弃 Decoder\n- BERT 编码器含 **12 层** Encoder Block，原始 Transformer 是 6 层\n- BERT 的位置编码是**可训练的**，Transformer 中是写死的正余弦编码\n- BERT 激活函数用 **GELU**，Transformer 用 ReLU","extend":"**面试追问：** 正因只用 Encoder，BERT 擅长理解类任务（分类、NER、句对关系）；GPT 只用 Decoder 做生成。","source":"bh-fixed"},{"id":432,"code":"NLP-8","category":"NLP基础","question":"BERT 怎么解决超长文本？了解 XLNet 吗？","answer":"BERT 限制序列长度不超过 512（本质不超过 510，扣除 [CLS]/[SEP]），处理更长文本的方案：\n- **截断**：只取前 510 字符，或只取后 510 字符\n- **不截断**：把超长文本切分成多段（每段 510），分别编码后取平均输出\n\n**XLNet** 是经典的排列语言模型，采用特殊的 mask 机制让 token 能同时\"看见\"前后文信息，且能应对更长的序列长度。","extend":"**面试追问：** 还可选 Longformer（稀疏注意力）、BigBird 等长序列模型；BERT 的绝对位置编码导致超长文本是硬限制。","source":"bh-fixed"},{"id":433,"code":"NLP-9","category":"NLP基础","question":"Word2Vec 是怎么计算的？霍夫曼树如何训练 Word2Vec？","answer":"**Word2Vec 计算：** 隐藏层张量是通过对上下文词向量**加权平均**得到的（CBOW），再经 softmax 输出预测词概率。\n\n**霍夫曼树训练：**\n- 先对全体语料做词频统计\n- 按词频构建霍夫曼树，每个词是叶子节点，词频对应节点权值\n- 实际训练采用层次 softmax、负采样两种优化方法\n\n在霍夫曼树中，隐藏层到输出层的 softmax 不是一步完成，而是沿树逐步判断（每个中间节点是一次逻辑回归，路径标记 0/1），叶子节点对应最终词，路径上概率累乘得到词概率。\n\n**两大优点：** 计算量从 N 降到 logN；越靠近根节点路径越短、参数更新越快，加速训练收敛。","extend":"**面试追问：** 层次 softmax 用霍夫曼树替代 softmax 降低复杂度，负采样用少量负样本近似，两者都是为了加速大规模词表训练。","source":"bh-fixed"},{"id":434,"code":"NLP-10","category":"NLP基础","question":"BERT 不加 CRF 和 BERT+CRF 做 NER 会有什么差别？","answer":"有相关论文，但特定数据集上的结论不能直接推广到生产实际。\n\n**BERT 不加 CRF：** 每个 token 独立做 softmax 分类，可能出现标签序列不合法（如 I-PER 出现在 B-PER 之前）。\n\n**BERT+CRF：** CRF 层建模标签间的转移约束，保证输出合法的标签序列；且能让模型更快收敛，用更小的数据集得到最优结果。\n\n实践中 NER 这类序列标注任务通常加 CRF 更稳。","extend":"**面试追问：** 也可以不加 CRF 而用指针网络/span 分类（如 BERT+Span 抽取）做 NER，近年倾向直接预测实体边界。","source":"bh-fixed"},{"id":435,"code":"NLP-11","category":"NLP基础","question":"AI 医生项目相关面试题","answer":"**1) 为什么不用 MySQL 用 Neo4j？**\nAI 领域 Neo4j 更适配，选型时考虑到后期的多级查询、多跳推理，都是 Neo4j 更合适。图结构数据用 MySQL 多表匹配查询速度跟不上。\n\n**2) CRF 太大为什么不用规则？**\n医疗数据纷繁复杂，虽用了一部分规则，但规则表扩展太快、无法覆盖足够多 case，用 CRF 更优，泛化性更强。\n\n**3) 图谱样式、内容、怎么用、为什么提取这些内容？**\n多种类型的节点（疾病、症状、药品、饮食、康复等），提取的内容都是为了后续的查询与问答做准备。","extend":"**面试追问：** 项目题要讲清「背景 → 架构 → 自己职责 → 难点与优化」，突出图数据库选型的对比论证。","source":"bh-fixed"},{"id":436,"code":"NLP-12","category":"NLP基础","question":"文本摘要项目相关面试题","answer":"**1) 讲讲数据增强有哪些，原理是什么？**\n- 单词替换、回译数据法、半监督学习法\n- 对比学习\n\n**2) fasttext 这个项目 embedding_dim 是多少，为何如此设置？**\nembedding_dim = 300 或 200 都可以，按经验值给定。\n\n**3) 图谱有什么作用，为什么不用 MySQL？**\nNeo4j 更适配多级查询、多跳推理；图结构数据用 MySQL 多表匹配查询速度跟不上。\n\n**4) PGN 架构是怎么实现的，是一个网络吗？coverage 机制为什么能解决 OOV 问题？**\nPGN（指针生成网络）是 seq2seq + 指针网络的结合，既可从词表生成，也可从原文复制，缓解 OOV（未登录词）。","extend":"**面试追问：** 文本摘要分抽取式与生成式；PGN 的 coverage 机制惩罚重复关注同一位置，缓解生成重复问题。","source":"bh-fixed"},{"id":437,"code":"NLP-13","category":"NLP基础","question":"传智大脑项目相关面试题","answer":"**1) 怎么划分数据，train_test_split 有什么问题，数据集有多少特征，离散还是连续？**\ntrain_test_split 有时划分不均匀；特征类型有的离散、有的连续，需按具体数据集说明。\n\n**2) 数据预处理做了哪些操作？数据分析做了哪些展示？用 train_test_split 合理吗？用了哪些集成学习？**\n主要是去除噪声、去重；分析做了 max_length 分布、标签数量分布，用柱状图/折线图展示。\n\n**3) 要求从头到尾讲一遍项目流程，从项目背景开始讲起。**\n按项目背景 → 数据 → 建模 → 评估 → 上线 的完整流程讲，突出个人负责部分与难点解决。","extend":"**面试追问：** 项目题务必准备完整流程讲述（背景/数据/建模/评估/部署），并准备 2-3 个技术难点及解法。","source":"bh-fixed"}]);
