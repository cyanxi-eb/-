# 面试背记学习卡 v2.9 —— 内容扩充与分类重组计划

> 版本：v2.9（v2.8 之后的第一次内容大扩充 + 分类重组）
> 数据来源：`C:\LLM0903`（LangChain/RAG 实战代码）+ `C:\Users\Administrator\Desktop\技术面试学习笔记-0907`（Git/RAG/Redis/LangChain 四份详解）+ 网搜补充
> 状态：待用户审阅确认

---

## 一、背景与目标

根据 `llm.txt` 的更新清单，本次迭代要完成：

1. **分类拆分**：把「大模型应用」（18 题）拆成 `大语言模型` / `RAG` / `LangChain` 三个子分类
2. **扩展 LangChain**：补充 Model I/O、三类模型、Models 层核心价值等
3. **新增 Git 分类**：把 SK-9 及新 git 题独立成「Git」分类，覆盖原理 + 常用命令
4. **扩展 RAG**：补充全流程、文档解析、分块技术细节
5. **Redis 缓存**：补充穿透/击穿/雪崩的细节方案（架构与三大问题已有覆盖）
6. **更换分类图标**：把 🏷 换成用户指定 emoji
7. **修复图片失效**：SK-6 引用的 `figures/fig_234422ee16.png` 不存在

---

## 二、现状分析（244 题，19 个分类）

| 相关分类 | 现有题量 | 说明 |
|---------|---------|------|
| 大模型应用 | 18 | 混着 LLM / RAG / LangChain，需拆分 |
| 缓存与Redis | 8 | DB-51/53/54/64/65/66/67/68，三大缓存问题已有覆盖 |
| 软技能 | 9 | SK-1~SK-9，其中 SK-9 是 git 题需迁出 |

**code 前缀现状**：`LLM-` 最大到 20，`SK-` 最大到 9，`DB-` 最大到 68。前缀对应「知识领域」，分类对应「展示分组」，两者非一一映射（如 `PY` 前缀覆盖 4 个分类）。

---

## 三、分类重组方案

### 3.1 拆分「大模型应用」→ 三个子分类

| 现有题目 | 主题 | 归属新分类 |
|---------|------|-----------|
| NLP-1 | BERT | 大语言模型 |
| NLP-7 | BERT vs Transformer | 大语言模型 |
| LLM-1 | 低代码平台 | 大语言模型 |
| LLM-8 | 大模型幻觉 | 大语言模型 |
| LLM-9 | Agent | 大语言模型 |
| LLM-10 | LoRA | 大语言模型 |
| LLM-13 | vLLM | 大语言模型 |
| LLM-15 | BERT 输入输出 | 大语言模型 |
| LLM-2 | RAG 是什么 | RAG |
| LLM-3 | RAG 工作原理 | RAG |
| LLM-5 | 分块 chunking | RAG |
| LLM-6 | 向量模型/向量库 | RAG |
| LLM-7 | Rerank/混合检索 | RAG |
| LLM-19 | RAG vs 微调 | RAG |
| LLM-20 | PDF 处理 | RAG |
| LLM-16 | LangChain 是什么 | LangChain |
| LLM-17 | LCEL | LangChain |
| LLM-18 | LangChain RAG 代码 | LangChain |

> 拆分后：大语言模型 8 题、RAG 7 题、LangChain 3 题。**code 前缀全部保留不变**（符合「避免轻易重命名」原则），仅改 `category` 字段。

### 3.2 新增「Git」分类

- 把 SK-9（git 分支工作流）迁出软技能 → 归入 Git 分类
- 新增 git 原理 + 常用命令题目

---

## 四、新增题目清单

> 每题结构：`code | 分类 | 星级 | bank | 题目`

### 4.1 Git 分类（新增 10 题，前缀 `GIT`）

| code | 星级 | bank | 题目 | 答案要点 |
|------|------|------|------|---------|
| GIT-1 | 5 | 4 | Git 三大区域是什么？数据如何流转？ | 工作区/暂存区/本地仓库；add→commit；restore 逆向 |
| GIT-2 | 5 | 4 | Git 底层三大对象模型？ | blob/tree/commit；SHA-1 寻址；快照非 diff |
| GIT-3 | 5 | 4 | HEAD、分支、Tag 三者的本质区别？ | 都是指针；HEAD 指分支、分支可变、Tag 不可变 |
| GIT-4 | 5 | 4 | git reset 和 git revert 的区别？ | reset 改写历史、revert 反向 commit；公共分支用 revert |
| GIT-5 | 5 | 4 | git merge 和 git rebase 的区别？ | merge 保留分叉、rebase 线性化 |
| GIT-6 | 4 | 3 | 常见分支管理策略有哪些？ | Git Flow / GitHub Flow / GitLab Flow |
| GIT-7 | 4 | 3 | git stash 的场景与用法？ | 临时保存未提交修改；pop/apply/drop |
| GIT-8 | 4 | 3 | git pull 和 git fetch 的区别？ | fetch 只拉不合并、pull=fetch+merge |
| GIT-9 | 4 | 3 | 合并冲突如何解决？ | 手动编辑→add→commit |
| GIT-10 | 3 | 2 | .gitignore 的作用与常见规则？ | 忽略系统/编辑器/依赖/构建产物/.env |

### 4.2 LangChain 分类（新增 7 题，前缀 `LLM`）

| code | 星级 | bank | 题目 | 答案要点 |
|------|------|------|------|---------|
| LLM-21 | 5 | 4 | LangChain 的 Model I/O 是什么？ | 提示词模板 + 模型调用 + 输出解析三部分 |
| LLM-22 | 5 | 4 | LangChain 三类模型（LLM/ChatModel/Embeddings）区别？ | 非对话/对话/嵌入；输入输出差异 |
| LLM-23 | 4 | 3 | LangChain Models 层核心价值？ | 统一封装各类模型接口、换模型零成本 |
| LLM-24 | 4 | 3 | PromptTemplate 和 ChatPromptTemplate 区别？ | 字符串模板 vs 多角色消息 |
| LLM-25 | 4 | 3 | 什么是 token？生成机制？ | 最小处理单元；链式预测；按 token 计费 |
| LLM-26 | 4 | 3 | LangChain Memory 模块作用？ | 多轮对话记忆；Buffer/Summary/Window |
| LLM-27 | 5 | 4 | 什么是 ReAct Agent？ | 推理+行动循环；自主选工具 |

### 4.3 RAG 分类（新增 7 题，前缀 `LLM`）

| code | 星级 | bank | 题目 | 答案要点 |
|------|------|------|------|---------|
| LLM-28 | 5 | 4 | RAG 文档解析有哪些技术难点？ | 复杂排版/表格/扫描件 OCR/跨页合并 |
| LLM-29 | 5 | 4 | 分块策略有哪些？各自优缺点？ | 固定长度/递归/语义/结构化/LLM 分块 |
| LLM-30 | 4 | 3 | Embedding 模型如何选型？ | 维度/中文效果 bge/m3e/费用 |
| LLM-31 | 4 | 3 | Rerank 重排序原理与价值？ | Cross-Encoder 交叉编码、精排 top-N |
| LLM-32 | 4 | 3 | 混合检索（向量+BM25）如何融合？ | 语义 + 关键词互补、加权融合 |
| LLM-33 | 4 | 3 | Query 改写 / HyDE / Multi-Query 是什么？ | 检索前优化、假设文档嵌入、多角度召回 |
| LLM-34 | 3 | 2 | GraphRAG 是什么？适用场景？ | 知识图谱增强、跨文档综合推理 |

### 4.4 Redis 分类（新增 3 题，前缀 `DB`）

| code | 星级 | bank | 题目 | 答案要点 |
|------|------|------|------|---------|
| DB-69 | 4 | 3 | 布隆过滤器的原理与误判？ | 多哈希映射位数组、存在性判断、有误判 |
| DB-70 | 4 | 3 | 缓存击穿的「逻辑过期」方案如何实现？ | 物理不设 TTL、异步刷新、无锁无阻塞 |
| DB-71 | 4 | 3 | Redis 哨兵如何选主？Cluster 如何分片？ | replica-priority>偏移量>runid；16384 哈希槽 |

> **合计新增：10 + 7 + 7 + 3 = 27 题**，题库 244 → 271 题。

---

## 五、编号与字段规范

- code 遵循 `{前缀}-{N}` 不补零（沿用现状）
- Git 题用新前缀 `GIT`；RAG/LangChain 新题沿用 `LLM` 前缀（避免前缀爆炸）
- `source` 统一填 `0907`（技术面试笔记来源）或 `LLM0903`
- `star` 1–5 星；`bank` 沿用「值越大越核心」，4=必刷核心，1=拓展外围

---

## 六、图标更换

- 现状：分类筛选按钮与弹层标题用的是 🏷（`index.html` 第 70、76 行）
- 候选：🧩 / 🗒 / 📕 / 📂 / 📁 / 📖 / 🔖（用户提供 7 选 1）
- 建议：📂（文件夹，贴合「分类」语义）
- **需用户确认选哪个**

---

## 七、图片失效修复（SK-6）

- 现象：SK-6 的 answer 里引用了 `./figures/fig_234422ee16.png`，但 `figures/` 目录为空（git 历史中也从未提交过该图），图片已丢失
- 修复方案（二选一，需确认）：
  - **方案 A**：删除 SK-6 里的图片引用行（`![图2](...)`），保留纯文字内容 —— 简单，无残留
  - **方案 B**：补一张示意图 —— 原图已丢失，无法复原

---

## 八、构建与部署

1. 修改 `questions.json`（新增 27 题 + 分类字段调整 + SK-6 图片修复 + SK-9 迁移）
2. 修改 `src/index.html`（分类图标）
3. bump `build-v25.cjs` 的 `VERSION='2.9'` + SW `CACHE` 常量（`fc-v34`）
4. `node build-v25.cjs` 重新构建
5. commit + push（Actions 自动部署）

---

## 九、待确认决策点

1. **分类图标**：7 选 1（我建议 📂）
2. **新增题量**：27 题是否合适？是否需要精简/增补
3. **SK-9 迁移**：code 保留 `SK-9` 还是改 `GIT-1`？（我建议保留 SK-9，仅改 category，避免重命名）
4. **SK-6 图片**：删除引用（方案 A）还是补图？
5. **分类命名**：`大语言模型` / `RAG` / `LangChain` 三个名字是否 OK？
